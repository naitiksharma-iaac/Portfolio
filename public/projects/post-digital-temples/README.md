# Post Digital Temples media

Put still images in `images` and GIFs in `gifs`.

Useful filename prefixes: `hero`, `typology`, `precedent`, `parametric`, `dataset`, `gan`, `voxel`, `dataset-a`, `failure`, `outlier`, `dataset-b`, `result`, `animation`.
