# Charge Pavilion media

Put still images in `images` and GIFs in `gifs`.

Useful filename prefixes: `hero`, `minimal`, `form`, `growth`, `climate`, `site`, `design`, `optimisation`, `structural`, `stress`, `model`, `final`, `animation`.
